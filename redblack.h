#include <stdlib.h>
#include <stdio.h>

#define BLACK 0
#define RED 1


typedef struct lista{
  unsigned long chave;
  int ind;
  struct lista * prox;
}rep;

typedef struct redblack{
  unsigned long naochave
  rep *rep_chave;
  struct tAVL * dir;
  struct tAVL * esq;
  int fb;
}rb;
